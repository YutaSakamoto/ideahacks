<div class="row">
 <div class="col-md-8 col-md-offset-3">
    <% @category.category_topic.each do |category_topic| %>
    <ul>
    <li><%= category_topic.topic.title %></li>
    <li><%= simple_format category_topic.topic.description %></li>
    </ul>
    <%end%>
 </div>
</div>
